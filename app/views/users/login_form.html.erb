<%# Do I need this after @user???? , url: login_path %>
<%= form_with model: @user do |f| %>
  <div>
    <%= f.label :user_name %>
    <%= f.text_field :user_name %>
  </div>
  <%= f.submit "Log in", class: "btn btn-primary" %>
<% end %>