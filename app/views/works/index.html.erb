<main>
  <h2>List of Works</h2>
  <%= render partial: 'work_table', locals: { category: 'album' } %>
  <%= render partial: 'work_table', locals: { category: 'book' } %>
  <%= render partial: 'work_table', locals: { category: 'movie' } %>
  <%= link_to 'View top media', root_path, class: "btn btn-secondary" %>
  <%= link_to 'Add a new work', new_work_path, class: "btn btn-primary" %>
</main>